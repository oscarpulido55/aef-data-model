CREATE TABLE `${exposure_project_id}.${exposure_dataset_id}.fact_sales`
(
    location_id INTEGER,
    product_id INTEGER,
    sales_value INTEGER,
    sales_cost INTEGER,
    sales_revenue INTEGER,
    sales_dt DATE
)
    PARTITION BY sales_dt
    OPTIONS (
        require_partition_filter = TRUE);