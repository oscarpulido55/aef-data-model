CREATE TABLE `${curated_project_id}.${curated_dataset_id}.sales`
(
    location_id INTEGER,
    product_id INTEGER,
    sales_value INTEGER,
    sales_cost INTEGER,
    sales_dt DATE
)
PARTITION BY sales_dt
OPTIONS (require_partition_filter = TRUE);